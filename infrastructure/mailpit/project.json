{
  "name": "shibuya-mailpit",
  "projectType": "library",
  "targets": {
    "forge": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker compose -p shibuya-mailpit up -d",
        "cwd": "infrastructure/mailpit"
      }
    },
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker compose -p shibuya-mailpit start",
        "cwd": "infrastructure/mailpit"
      }
    },
    "stop": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "infrastructure/mailpit",
        "commands": ["docker compose -p shibuya-mailpit stop"]
      }
    },
    "melt": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "infrastructure/mailpit",
        "commands": ["docker compose -p shibuya-mailpit -v down"],
        "parallel": false
      }
    }
  }
}
