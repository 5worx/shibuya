{
  "name": "kpn-database",
  "projectType": "library",
  "targets": {
    "forge": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker compose -p kpn-database up -d kpn-db kpn-pgadmin",
        "cwd": "infrastructure/database"
      }
    },
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker compose -p kpn-database start",
        "cwd": "infrastructure/database"
      }
    },
    "stop": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["docker compose -p kpn-database stop"],
        "cwd": "infrastructure/database",
        "parallel": false
      }
    },
    "melt": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "infrastructure/database",
        "commands": ["docker compose -p kpn-database -v down"],
        "parallel": false
      }
    }
  }
}
